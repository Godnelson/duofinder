services:
  db:
    image: postgres:16
    container_name: duofinder-db
    environment:
      POSTGRES_USER: duo
      POSTGRES_PASSWORD: duo
      POSTGRES_DB: duofinder
    ports:
      - "5432:5432"
    volumes:
      - duofinder_pg:/var/lib/postgresql/data

volumes:
  duofinder_pg:
